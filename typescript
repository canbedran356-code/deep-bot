// Örnek: Botun temel yapısı
import { Telegraf, Context } from 'telegraf';
import { message } from 'telegraf/filters';

const bot = new Telegraf(process.env.BOT_TOKEN);

// Kürtçe komutlar
bot.command('qedexe', async (ctx) => {  // /ban
  // Reply veya mention kontrolü
  // Yetki kontrolü
  // Ban işlemi
  // Log kanalına yaz
});

bot.command('rabicûyî', async (ctx) => {  // /unban
  // ...
});

// Butonlu admin paneli
bot.command('adminpanel', async (ctx) => {
  await ctx.reply('📋 Panela Adminê - Hilbijêre:', {
    reply_markup: {
      inline_keyboard: [
        [{ text: '🔨 Qedexe kirin', callback_data: 'ban' }],
        [{ text: '🔓 Rabicûyî', callback_data: 'unban' }],
        [{ text: '⚠️ Hişyarî dan', callback_data: 'warn' }],
      ]
    }
  });
});

bot.launch();
